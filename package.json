{
  "name": "ffmpegb",
  "version": "1.0.0",
  "description": "ffmpeg.wasm CLI wrapper powered by Bun workers",
  "author": "opennexus <noreply@opennexus.xyz>",
  "module": "src/cli.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "generate-assets": "bun run scripts/generate-assets.ts",
    "build": "bun run generate-assets && bun build --compile src/cli.ts --outfile ffmpegb",
    "bench": "bun run scripts/bench.ts",
    "test": "bun run test/suite.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "bin": {
    "ffmpegb": "./src/cli.ts"
  }
}
